Multimodal Context:
Query Image ({query_image}): [Image] (Target)
Diagnosis ({diagnosis}): [JSON] (Defect Context)
Current Parameters ({current_parameters}): [JSON] (Current Process State)
Reference Parameters ({reference_parameters}): [List] (Per-Parameter Mean from Top-k Retrieved Standard Exemplars)
Physics Rules ({physics_rules}): [Text] (Physics-Guided Directives)

CoT Reasoning Logic:
You must adhere to the following Chain-of-Thought reasoning path:

Step 1 (Semantic Understanding): Analyze {query_image} and {diagnosis} to verify the defect type and severity. Establish the semantic context of the failure (e.g., "severe clustered surface porosity").

Step 2 (Parametric Gap Analysis): Contrast the current process parameters against the statistical benchmark defined by {reference_parameters}. Articulate the specific parametric deviation (e.g., "Voltage exceeds the statistical benchmark by 4 V").

Step 3 (Physics-Guided Derivation): Apply the physics-guided directives from {physics_rules} to the identified gap. Map the parametric deviation to a corrective action that satisfies the physical constraints, and synthesize the final optimization proposal containing specific directional adjustments.
